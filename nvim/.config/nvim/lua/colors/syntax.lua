local palette = require("colors.palette")
local utils = require("colors.utils")
local prop = utils.prop

return {
    Boolean        = palette.Purple,
    Number         = palette.Purple,
    Float          = palette.Purple,
    PreProc        = palette.Purple,
    PreCondit      = palette.Purple,
    Include        = palette.Purple,
    Define         = palette.Purple,
    Conditional    = palette.Red,
    Repeat         = palette.Red,
    Keyword        = palette.Red,
    Typedef        = palette.Red,
    Exception      = palette.Red,
    Statement      = palette.Red,
    Error          = palette.Red,
    StorageClass   = palette.Orange,
    Tag            = palette.Orange,
    Label          = palette.Orange,
    Structure      = palette.Orange,
    Operator       = palette.Orange,
    Title          = palette.Orange,
    Special        = palette.Yellow,
    SpecialChar    = palette.Yellow,
    Type           = palette.Yellow,
    Function       = palette.Green,
    Method       = palette.Yellow,
    String         = palette.Green,
    Character      = palette.Green,
    Constant       = palette.Aqua,
    Macro          = palette.Aqua,
    Identifier     = palette.Blue,
    Comment        = palette.Grey,
    SpecialComment = palette.Grey,
    Todo           = palette.Purple,
    Delimiter      = palette.Fg0,
    Ignore         = palette.Grey,
    Underlined     = prop(palette.none,palette.none,'underline'),
}
